{
  "name": "backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "cd ../frontend && npm install && npx vite build --outDir=../backend/frontend/dist"
  },
  "dependencies": {
    "encore.dev": "^1.48.8"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}